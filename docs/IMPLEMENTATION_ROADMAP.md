# BILLI PLATFORM: IMPLEMENTATION & DEPLOYMENT ROADMAP

> **Superseded.** This roadmap described an earlier architecture generation
> (GCP Terraform infra, Firestore/Spanner/AlloyDB, Cloud Functions, a Flutter
> mobile app) that was never actually built out — the `cloud-functions/` and
> Terraform files it referenced were removed from the repo as dead code.
>
> **For the real, running platform — 13 local Express/TypeScript services,
> a Gemini-backed context engine, and the `web-app/` frontend — see
> [`README.md`](../README.md).** Quick start is one command: `npm start`.
>
> Kept here for historical reference only.
